#include <bits/stdc++.h>

#include "cpu.hpp"

int main() {
    // initial
    CPU t;

    t.clock_cycle();
/*
    while (1) {
        // 1 clock cycle
        t.clock_cycle();

        // draw
        if (t.get_draw_flag()) {
            t.draw();
        }

        // maybe deal with the key
    }
*/
}
